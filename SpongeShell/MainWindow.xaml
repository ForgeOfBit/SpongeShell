<Window
    x:Class="SpongeShell.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpongeShell"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SpongeShell"
    MinWidth="640"
    MinHeight="400">

    <Grid Background="#0C0C0C">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#1E1E1E" Padding="12,8" BorderBrush="#3C3C3C" BorderThickness="0,0,0,1">
            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <TextBlock Text="SpongeShell" FontSize="14" Foreground="#CCCCCC" FontWeight="SemiBold"/>
                <TextBlock x:Name="ShellStatusText" Text="Shell baslatiyor..." Foreground="#808080" FontSize="12" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>
        <ScrollViewer x:Name="TerminalScroll" Grid.Row="1" Padding="16,12" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" Background="Transparent">
            <TextBox x:Name="TerminalOutput" IsReadOnly="True" AcceptsReturn="True" TextWrapping="NoWrap" FontFamily="Consolas" FontSize="14" Foreground="#CCCCCC" Background="#0C0C0C" BorderThickness="0" Padding="0" MinWidth="600" MinHeight="300" KeyDown="TerminalOutput_KeyDown" KeyUp="TerminalOutput_KeyUp"/>
        </ScrollViewer>
    </Grid>
</Window>
